# ==========================================================================
# CRE WORKFLOW SETTINGS FILE
# ==========================================================================
staging-settings:
  user-workflow:
    workflow-name: "syndicatelend-interest-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

# ==========================================================================
production-settings:
  user-workflow:
    workflow-name: "syndicatelend-interest-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "../secrets.yaml"

# Negative test: the agent endpoint returns an altered notice; the enclave must abort.
tamper-settings:
  user-workflow:
    workflow-name: "syndicatelend-interest-tamper"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.tamper.json"
    secrets-path: "../secrets.yaml"
