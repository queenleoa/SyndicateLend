import { InstitutionWorkspace } from "@/components/institution-workspace";

export const metadata = { title: "Institution · Wallet & permissions" };

export default function InstitutionPage() {
  return <InstitutionWorkspace />;
}
